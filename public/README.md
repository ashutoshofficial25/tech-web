# Public Directory

This directory contains static assets and SEO-related files that are copied directly to the build folder during the build process.

Files in this directory include:

- robots.txt - Instructions for search engine crawlers
- sitemap.xml - Site structure information for search engines
- manifest.json - Web app manifest for PWA capabilities
- .htaccess - Server configuration for redirects and caching
